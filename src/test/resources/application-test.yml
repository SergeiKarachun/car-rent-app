spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
        hbm2ddl.auto: validate
    open-in-view: false
logging:
  level:
    root: INFO
